field_classes:
  RunMode:
    constraints:
      choices:
which is better?
do we need name value and label?
could convert Method_One into "Method One"
why would we need method_1

        - name: method_1
          label: Method One
        - name: method_2
          label: Method Two
        - name: method_3
          label: Method Three

        "Method One": method_1
        "Method Two": method 2
        "Method Three": method_3

This is too verbose. It could be choices is just a list of strings or a dict
color_map:
  choices:
  - code: Cividis
    display: Cividis
  - code: Blues
    display: Blues
  - code: Inferno
    display: Inferno
  - code: Turbo
    display: Turbo
  - code: Viridis
    display: Viridis


    ui:
      label: Run Mode
      widget: select
    default: method_1
  Output:
    ui:
      writable: False
      # TODO(robnagler) better name, seems like it's just a parameter on the widget
      widget: static


fields:
  save_button:
    class: Button
    ui:
      html_class: primary
      label: Save
  revert_button:
    class: Button
    ui:
      html_class: danger
      label: Revert
  output1:
    class: simple.Output
    ui:
      label: 'Output #1'
  output2:
    class: simple.Output
    ui:
      label: 'Output #2'
  run_mode:
    class: simple.RunMode
  increment:
    # Inline type
    class: Integer
    constraints:
      min: 1
      nullable: False
    ui:
      label: Increment Amount
    value: 5

layout:
  columns:
  - layout: col-lg-6 offset-lg-3 col-xl-4 offset-xl-4
    rows:
    - name: run_mode
    - name: increment
    - name: divisor
    - columns:
      - layout: col-sm-6
        rows:
        - cell:
            - save_button
            - revert_button
    - name: output1
    - name: output2
